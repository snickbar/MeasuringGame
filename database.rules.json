{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      ".read": false,
      ".write": false,
      "$roomCode": {
        ".read": "$roomCode.matches(/^[A-Z0-9]{4}$/)",
        ".write": "$roomCode.matches(/^[A-Z0-9]{4}$/)"
      }
    }
  }
}
